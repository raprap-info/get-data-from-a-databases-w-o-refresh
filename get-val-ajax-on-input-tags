$(document).ready(function() {
    $("input").keyup(function() {
        var name
       $. post("suggestions.php", {
            suggestion: name
       }, function(data, status) {
            $("#test").html(data);
        });
    });
                   $("input").val(); 
                          I
});
